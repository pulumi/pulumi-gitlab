// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * The `gitlab.ProjectContainerTagProtection` resource allows managing the lifecycle of a container tag protection rule.
 *
 * You can use a regular expression to protect multiple container tags with the same container protection rule.
 * You have to **either set** (both options are mutually exclusive):
 *
 * - Only `immutable = true` which allows any user with `developer` role to create tags matching the regex but makes the object unchangeable once pushed ;
 * - Both `minimumAccessLevelForPush` and `minimumAccessLevelForDelete` attributes to mark the container tag as mutable only with regards to the given access levels.
 *
 * **Upstream API**: [GitLab GraphQL API documentation](https://docs.gitlab.com/api/graphql/reference/#mutationcreatecontainerprotectiontagrule)
 *
 * **Protected tags**: [General documentation](https://docs.gitlab.com/user/packages/container_registry/protected_container_tags/)
 *
 * **Immutable tags**: [General documentation](https://docs.gitlab.com/user/packages/container_registry/immutable_container_tags/)
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gitlab from "@pulumi/gitlab";
 *
 * const example = new gitlab.ProjectContainerTagProtection("example", {
 *     project: "123",
 *     tagNameRegex: "^v[0-9]+$",
 *     immutable: true,
 * });
 * const _protected = new gitlab.ProjectContainerTagProtection("protected", {
 *     project: "123",
 *     tagNameRegex: "^v[0-9]+\\-rc[0-9]+$",
 *     minimumAccessLevelForPush: "MAINTAINER",
 *     minimumAccessLevelForDelete: "OWNER",
 * });
 * ```
 *
 * ## Import
 *
 * Starting in Terraform v1.5.0, you can use an import block to import `gitlab.ProjectContainerTagProtection`. For example:
 *
 * Importing using the CLI is supported with the following syntax:
 *
 * GitLab project container tag protection rules can be imported using an id made up of `<project_id>:<protection_rule_id>`, for example:
 *
 * ```sh
 * $ pulumi import gitlab:index/projectContainerTagProtection:ProjectContainerTagProtection example 123:456
 * ```
 */
export class ProjectContainerTagProtection extends pulumi.CustomResource {
    /**
     * Get an existing ProjectContainerTagProtection resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ProjectContainerTagProtectionState, opts?: pulumi.CustomResourceOptions): ProjectContainerTagProtection {
        return new ProjectContainerTagProtection(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'gitlab:index/projectContainerTagProtection:ProjectContainerTagProtection';

    /**
     * Returns true if the given object is an instance of ProjectContainerTagProtection.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ProjectContainerTagProtection {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ProjectContainerTagProtection.__pulumiType;
    }

    /**
     * Whether the container tag is immutable. If set, the container tag cannot be deleted or overwritten. Conflicts with `minimumAccessLevelForPush` and `minimumAccessLevelForDelete`.
     */
    declare public readonly immutable: pulumi.Output<boolean>;
    /**
     * Minimum GitLab access level required to delete protected container tags in the container registry. Marks the container tag as protected. Valid values are: `MAINTAINER`, `OWNER`, `ADMIN`.
     */
    declare public readonly minimumAccessLevelForDelete: pulumi.Output<string>;
    /**
     * Minimum GitLab access level required to push protected container tags to the container registry. Marks the container tag as protected. Valid values are: `MAINTAINER`, `OWNER`, `ADMIN`.
     */
    declare public readonly minimumAccessLevelForPush: pulumi.Output<string>;
    /**
     * ID or URL-encoded path of the project.
     */
    declare public readonly project: pulumi.Output<string>;
    /**
     * Unique ID of the protection rule.
     */
    declare public /*out*/ readonly protectionRuleId: pulumi.Output<number>;
    /**
     * Container tag path pattern protected by the protection rule. Wildcard character * allowed. Tag path pattern should start with the project's full path.
     */
    declare public readonly tagNameRegex: pulumi.Output<string>;
    declare public readonly timeouts: pulumi.Output<outputs.ProjectContainerTagProtectionTimeouts | undefined>;

    /**
     * Create a ProjectContainerTagProtection resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ProjectContainerTagProtectionArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ProjectContainerTagProtectionArgs | ProjectContainerTagProtectionState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ProjectContainerTagProtectionState | undefined;
            resourceInputs["immutable"] = state?.immutable;
            resourceInputs["minimumAccessLevelForDelete"] = state?.minimumAccessLevelForDelete;
            resourceInputs["minimumAccessLevelForPush"] = state?.minimumAccessLevelForPush;
            resourceInputs["project"] = state?.project;
            resourceInputs["protectionRuleId"] = state?.protectionRuleId;
            resourceInputs["tagNameRegex"] = state?.tagNameRegex;
            resourceInputs["timeouts"] = state?.timeouts;
        } else {
            const args = argsOrState as ProjectContainerTagProtectionArgs | undefined;
            if (args?.project === undefined && !opts.urn) {
                throw new Error("Missing required property 'project'");
            }
            if (args?.tagNameRegex === undefined && !opts.urn) {
                throw new Error("Missing required property 'tagNameRegex'");
            }
            resourceInputs["immutable"] = args?.immutable;
            resourceInputs["minimumAccessLevelForDelete"] = args?.minimumAccessLevelForDelete;
            resourceInputs["minimumAccessLevelForPush"] = args?.minimumAccessLevelForPush;
            resourceInputs["project"] = args?.project;
            resourceInputs["tagNameRegex"] = args?.tagNameRegex;
            resourceInputs["timeouts"] = args?.timeouts;
            resourceInputs["protectionRuleId"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ProjectContainerTagProtection.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ProjectContainerTagProtection resources.
 */
export interface ProjectContainerTagProtectionState {
    /**
     * Whether the container tag is immutable. If set, the container tag cannot be deleted or overwritten. Conflicts with `minimumAccessLevelForPush` and `minimumAccessLevelForDelete`.
     */
    immutable?: pulumi.Input<boolean>;
    /**
     * Minimum GitLab access level required to delete protected container tags in the container registry. Marks the container tag as protected. Valid values are: `MAINTAINER`, `OWNER`, `ADMIN`.
     */
    minimumAccessLevelForDelete?: pulumi.Input<string>;
    /**
     * Minimum GitLab access level required to push protected container tags to the container registry. Marks the container tag as protected. Valid values are: `MAINTAINER`, `OWNER`, `ADMIN`.
     */
    minimumAccessLevelForPush?: pulumi.Input<string>;
    /**
     * ID or URL-encoded path of the project.
     */
    project?: pulumi.Input<string>;
    /**
     * Unique ID of the protection rule.
     */
    protectionRuleId?: pulumi.Input<number>;
    /**
     * Container tag path pattern protected by the protection rule. Wildcard character * allowed. Tag path pattern should start with the project's full path.
     */
    tagNameRegex?: pulumi.Input<string>;
    timeouts?: pulumi.Input<inputs.ProjectContainerTagProtectionTimeouts>;
}

/**
 * The set of arguments for constructing a ProjectContainerTagProtection resource.
 */
export interface ProjectContainerTagProtectionArgs {
    /**
     * Whether the container tag is immutable. If set, the container tag cannot be deleted or overwritten. Conflicts with `minimumAccessLevelForPush` and `minimumAccessLevelForDelete`.
     */
    immutable?: pulumi.Input<boolean>;
    /**
     * Minimum GitLab access level required to delete protected container tags in the container registry. Marks the container tag as protected. Valid values are: `MAINTAINER`, `OWNER`, `ADMIN`.
     */
    minimumAccessLevelForDelete?: pulumi.Input<string>;
    /**
     * Minimum GitLab access level required to push protected container tags to the container registry. Marks the container tag as protected. Valid values are: `MAINTAINER`, `OWNER`, `ADMIN`.
     */
    minimumAccessLevelForPush?: pulumi.Input<string>;
    /**
     * ID or URL-encoded path of the project.
     */
    project: pulumi.Input<string>;
    /**
     * Container tag path pattern protected by the protection rule. Wildcard character * allowed. Tag path pattern should start with the project's full path.
     */
    tagNameRegex: pulumi.Input<string>;
    timeouts?: pulumi.Input<inputs.ProjectContainerTagProtectionTimeouts>;
}
