// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * The `gitlab.getArtifactFile` data source allows downloading a single artifact file from a specific job in the latest successful pipeline for a given reference (branch, tag, or commit).
 * **Upstream API**: [GitLab REST API docs](https://docs.gitlab.com/api/job_artifacts/#download-a-single-artifact-file-by-reference-name)
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gitlab from "@pulumi/gitlab";
 *
 * // Download a text artifact file from the latest successful pipeline
 * const config = gitlab.getArtifactFile({
 *     project: "namespace/myproject",
 *     job: "build-job",
 *     ref: "main",
 *     artifactPath: "config/settings.json",
 * });
 * export const configContent = config.then(config => config.content);
 * // Download a binary artifact file using base64 encoding
 * const binary = gitlab.getArtifactFile({
 *     project: "namespace/myproject",
 *     job: "build-job",
 *     ref: "v1.0.0",
 *     artifactPath: "dist/app.zip",
 * });
 * export const binaryContentBase64 = binary.then(binary => binary.contentBase64);
 * // Download artifact from a specific tag
 * const release = gitlab.getArtifactFile({
 *     project: "12345",
 *     job: "release-job",
 *     ref: "v2.1.0",
 *     artifactPath: "release-notes.txt",
 * });
 * // Download a larger artifact with custom size limit
 * const largeArtifact = gitlab.getArtifactFile({
 *     project: "namespace/myproject",
 *     job: "build-job",
 *     ref: "main",
 *     artifactPath: "dist/large-file.zip",
 *     maxSizeBytes: 20971520,
 * });
 * ```
 */
export function getArtifactFile(args: GetArtifactFileArgs, opts?: pulumi.InvokeOptions): Promise<GetArtifactFileResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("gitlab:index/getArtifactFile:getArtifactFile", {
        "artifactPath": args.artifactPath,
        "job": args.job,
        "maxSizeBytes": args.maxSizeBytes,
        "project": args.project,
        "ref": args.ref,
    }, opts);
}

/**
 * A collection of arguments for invoking getArtifactFile.
 */
export interface GetArtifactFileArgs {
    /**
     * Path to the artifact file within the job's artifacts archive. This path is relative to the archive contents (not the local filesystem). Ensure each `gitlab.getArtifactFile` data source in your configuration uses a unique artifactPath to avoid ambiguity.
     */
    artifactPath: string;
    /**
     * The name of the job.
     */
    job: string;
    /**
     * Maximum bytes to read from the artifact. Defaults to 10MB (10485760 bytes).
     */
    maxSizeBytes?: number;
    /**
     * The ID or URL-encoded path of the project.
     */
    project: string;
    /**
     * The name of the branch, tag, or commit SHA.
     */
    ref: string;
}

/**
 * A collection of values returned by getArtifactFile.
 */
export interface GetArtifactFileResult {
    /**
     * Path to the artifact file within the job's artifacts archive. This path is relative to the archive contents (not the local filesystem). Ensure each `gitlab.getArtifactFile` data source in your configuration uses a unique artifactPath to avoid ambiguity.
     */
    readonly artifactPath: string;
    /**
     * The content of the artifact file as a UTF-8 string. Use `contentBase64` for binary files.
     */
    readonly content: string;
    /**
     * The content of the artifact file as a base64-encoded string. Useful for binary files.
     */
    readonly contentBase64: string;
    /**
     * The ID of this datasource. In the format `<project>:<ref>:<job>:<artifact_path>`.
     */
    readonly id: string;
    /**
     * The name of the job.
     */
    readonly job: string;
    /**
     * Maximum bytes to read from the artifact. Defaults to 10MB (10485760 bytes).
     */
    readonly maxSizeBytes?: number;
    /**
     * The ID or URL-encoded path of the project.
     */
    readonly project: string;
    /**
     * The name of the branch, tag, or commit SHA.
     */
    readonly ref: string;
}
/**
 * The `gitlab.getArtifactFile` data source allows downloading a single artifact file from a specific job in the latest successful pipeline for a given reference (branch, tag, or commit).
 * **Upstream API**: [GitLab REST API docs](https://docs.gitlab.com/api/job_artifacts/#download-a-single-artifact-file-by-reference-name)
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gitlab from "@pulumi/gitlab";
 *
 * // Download a text artifact file from the latest successful pipeline
 * const config = gitlab.getArtifactFile({
 *     project: "namespace/myproject",
 *     job: "build-job",
 *     ref: "main",
 *     artifactPath: "config/settings.json",
 * });
 * export const configContent = config.then(config => config.content);
 * // Download a binary artifact file using base64 encoding
 * const binary = gitlab.getArtifactFile({
 *     project: "namespace/myproject",
 *     job: "build-job",
 *     ref: "v1.0.0",
 *     artifactPath: "dist/app.zip",
 * });
 * export const binaryContentBase64 = binary.then(binary => binary.contentBase64);
 * // Download artifact from a specific tag
 * const release = gitlab.getArtifactFile({
 *     project: "12345",
 *     job: "release-job",
 *     ref: "v2.1.0",
 *     artifactPath: "release-notes.txt",
 * });
 * // Download a larger artifact with custom size limit
 * const largeArtifact = gitlab.getArtifactFile({
 *     project: "namespace/myproject",
 *     job: "build-job",
 *     ref: "main",
 *     artifactPath: "dist/large-file.zip",
 *     maxSizeBytes: 20971520,
 * });
 * ```
 */
export function getArtifactFileOutput(args: GetArtifactFileOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetArtifactFileResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("gitlab:index/getArtifactFile:getArtifactFile", {
        "artifactPath": args.artifactPath,
        "job": args.job,
        "maxSizeBytes": args.maxSizeBytes,
        "project": args.project,
        "ref": args.ref,
    }, opts);
}

/**
 * A collection of arguments for invoking getArtifactFile.
 */
export interface GetArtifactFileOutputArgs {
    /**
     * Path to the artifact file within the job's artifacts archive. This path is relative to the archive contents (not the local filesystem). Ensure each `gitlab.getArtifactFile` data source in your configuration uses a unique artifactPath to avoid ambiguity.
     */
    artifactPath: pulumi.Input<string>;
    /**
     * The name of the job.
     */
    job: pulumi.Input<string>;
    /**
     * Maximum bytes to read from the artifact. Defaults to 10MB (10485760 bytes).
     */
    maxSizeBytes?: pulumi.Input<number>;
    /**
     * The ID or URL-encoded path of the project.
     */
    project: pulumi.Input<string>;
    /**
     * The name of the branch, tag, or commit SHA.
     */
    ref: pulumi.Input<string>;
}
