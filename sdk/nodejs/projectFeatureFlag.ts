// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * The `gitlab.ProjectFeatureFlag` resource manages the lifecycle of a project-level feature flag.
 *
 * Feature flags allow you to progressively roll out features using different strategies (e.g. default,
 * gradual rollout by user ID, or user lists).
 *
 * **Upstream API**: [GitLab REST API docs](https://docs.gitlab.com/api/feature_flags/)
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gitlab from "@pulumi/gitlab";
 *
 * const example = new gitlab.Project("example", {
 *     name: "example",
 *     visibilityLevel: "private",
 * });
 * // Minimal example: a feature flag with no rollout strategies.
 * const minimal = new gitlab.ProjectFeatureFlag("minimal", {
 *     project: example.id,
 *     name: "my_feature",
 * });
 * // Full example: a feature flag with a gradual rollout strategy.
 * const exampleProjectFeatureFlag = new gitlab.ProjectFeatureFlag("example", {
 *     project: example.id,
 *     name: "gradual_rollout_feature",
 *     description: "Gradual rollout of the new checkout flow",
 *     active: true,
 *     strategies: [{
 *         name: "gradualRolloutUserId",
 *         parameters: {
 *             percentage: "50",
 *         },
 *         scopes: [{
 *             environmentScope: "production",
 *         }],
 *     }],
 * });
 * // Example targeting a fixed set of users via a gitlab_project_feature_flag_user_list.
 * const betaTesters = new gitlab.ProjectFeatureFlagUserList("beta_testers", {
 *     project: example.id,
 *     name: "beta_testers",
 *     userXids: "user1,user2,user3",
 * });
 * const userListExample = new gitlab.ProjectFeatureFlag("user_list_example", {
 *     project: example.id,
 *     name: "user_list_feature",
 *     strategies: [{
 *         name: "gitlabUserList",
 *         userListId: betaTesters.listId,
 *     }],
 * });
 * ```
 *
 * ## Import
 *
 * Starting in Terraform v1.5.0, you can use an import block to import `gitlab.ProjectFeatureFlag`. For example:
 *
 * Importing using the CLI is supported with the following syntax:
 *
 * A GitLab Project Feature Flag can be imported using a key composed of
 * `<project>:<feature-flag-name>`, e.g.
 *
 * ```sh
 * $ pulumi import gitlab:index/projectFeatureFlag:ProjectFeatureFlag example "12345:my_feature"
 * ```
 */
export class ProjectFeatureFlag extends pulumi.CustomResource {
    /**
     * Get an existing ProjectFeatureFlag resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ProjectFeatureFlagState, opts?: pulumi.CustomResourceOptions): ProjectFeatureFlag {
        return new ProjectFeatureFlag(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'gitlab:index/projectFeatureFlag:ProjectFeatureFlag';

    /**
     * Returns true if the given object is an instance of ProjectFeatureFlag.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ProjectFeatureFlag {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ProjectFeatureFlag.__pulumiType;
    }

    /**
     * Whether the feature flag is active. Defaults to `true`.
     */
    declare public readonly active: pulumi.Output<boolean>;
    /**
     * The date and time the feature flag was created, in ISO 8601 format.
     */
    declare public /*out*/ readonly createdAt: pulumi.Output<string>;
    /**
     * The description of the feature flag.
     */
    declare public readonly description: pulumi.Output<string>;
    /**
     * The name of the feature flag.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * The ID or URL-encoded path of the project.
     */
    declare public readonly project: pulumi.Output<string>;
    /**
     * A set of feature flag strategies. Updating this set replaces every strategy it previously managed in a single API call: strategies no longer present are removed (via the GitLab API's `_destroy` flag) and the rest are recreated, but the feature flag itself (`name`, `description`, `active`, `createdAt`) is left untouched. Leaving `strategies` unconfigured (the default) leaves any existing strategies - including `gitlabUserList` bindings made outside of Terraform - alone.
     */
    declare public readonly strategies: pulumi.Output<outputs.ProjectFeatureFlagStrategy[] | undefined>;

    /**
     * Create a ProjectFeatureFlag resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ProjectFeatureFlagArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ProjectFeatureFlagArgs | ProjectFeatureFlagState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ProjectFeatureFlagState | undefined;
            resourceInputs["active"] = state?.active;
            resourceInputs["createdAt"] = state?.createdAt;
            resourceInputs["description"] = state?.description;
            resourceInputs["name"] = state?.name;
            resourceInputs["project"] = state?.project;
            resourceInputs["strategies"] = state?.strategies;
        } else {
            const args = argsOrState as ProjectFeatureFlagArgs | undefined;
            if (args?.project === undefined && !opts.urn) {
                throw new Error("Missing required property 'project'");
            }
            resourceInputs["active"] = args?.active;
            resourceInputs["description"] = args?.description;
            resourceInputs["name"] = args?.name;
            resourceInputs["project"] = args?.project;
            resourceInputs["strategies"] = args?.strategies;
            resourceInputs["createdAt"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ProjectFeatureFlag.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ProjectFeatureFlag resources.
 */
export interface ProjectFeatureFlagState {
    /**
     * Whether the feature flag is active. Defaults to `true`.
     */
    active?: pulumi.Input<boolean | undefined>;
    /**
     * The date and time the feature flag was created, in ISO 8601 format.
     */
    createdAt?: pulumi.Input<string | undefined>;
    /**
     * The description of the feature flag.
     */
    description?: pulumi.Input<string | undefined>;
    /**
     * The name of the feature flag.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The ID or URL-encoded path of the project.
     */
    project?: pulumi.Input<string | undefined>;
    /**
     * A set of feature flag strategies. Updating this set replaces every strategy it previously managed in a single API call: strategies no longer present are removed (via the GitLab API's `_destroy` flag) and the rest are recreated, but the feature flag itself (`name`, `description`, `active`, `createdAt`) is left untouched. Leaving `strategies` unconfigured (the default) leaves any existing strategies - including `gitlabUserList` bindings made outside of Terraform - alone.
     */
    strategies?: pulumi.Input<pulumi.Input<inputs.ProjectFeatureFlagStrategy>[] | undefined>;
}

/**
 * The set of arguments for constructing a ProjectFeatureFlag resource.
 */
export interface ProjectFeatureFlagArgs {
    /**
     * Whether the feature flag is active. Defaults to `true`.
     */
    active?: pulumi.Input<boolean | undefined>;
    /**
     * The description of the feature flag.
     */
    description?: pulumi.Input<string | undefined>;
    /**
     * The name of the feature flag.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The ID or URL-encoded path of the project.
     */
    project: pulumi.Input<string>;
    /**
     * A set of feature flag strategies. Updating this set replaces every strategy it previously managed in a single API call: strategies no longer present are removed (via the GitLab API's `_destroy` flag) and the rest are recreated, but the feature flag itself (`name`, `description`, `active`, `createdAt`) is left untouched. Leaving `strategies` unconfigured (the default) leaves any existing strategies - including `gitlabUserList` bindings made outside of Terraform - alone.
     */
    strategies?: pulumi.Input<pulumi.Input<inputs.ProjectFeatureFlagStrategy>[] | undefined>;
}
