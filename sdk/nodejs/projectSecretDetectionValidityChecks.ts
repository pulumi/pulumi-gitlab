// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * The `gitlab.ProjectSecretDetectionValidityChecks` resource allows managing whether secret detection validity checks are enabled on a project.
 *
 * > This resource requires a GitLab Ultimate license, and is available on GitLab.com, GitLab Self-Managed, and GitLab Dedicated.
 *
 * > [Validity checks](https://docs.gitlab.com/user/application_security/vulnerabilities/validity_check/) verify whether detected secrets are still active, reducing false positives in secret detection results.
 *
 * > When you destroy the resource, you can control if the validity checks setting is kept or not. Set `keepSettingsOnDestroy` to `true` (default) to leave the setting as last configured. Set `keepSettingsOnDestroy` to `false` to reset it to its pre-Terraform value. The original value is saved in state when you create the resource. You can change the `keepSettingsOnDestroy` value before destroying the resource to control this behavior.
 *
 * **Upstream API**: [GitLab GraphQL API docs](https://docs.gitlab.com/api/graphql/reference/index/#mutationsetvaliditychecks)
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as gitlab from "@pulumi/gitlab";
 *
 * const example = new gitlab.Project("example", {name: "example"});
 * const exampleProjectSecretDetectionValidityChecks = new gitlab.ProjectSecretDetectionValidityChecks("example", {
 *     project: example.id,
 *     validityChecksEnabled: true,
 * });
 * ```
 *
 * ## Import
 *
 * Starting in Terraform v1.5.0, you can use an import block to import `gitlab.ProjectSecretDetectionValidityChecks`. For example:
 *
 * Importing using the CLI is supported with the following syntax:
 *
 * GitLab project secret detection validity checks can be imported using the project ID or full path, e.g.
 *
 * ```sh
 * $ pulumi import gitlab:index/projectSecretDetectionValidityChecks:ProjectSecretDetectionValidityChecks example "12345"
 * ```
 */
export class ProjectSecretDetectionValidityChecks extends pulumi.CustomResource {
    /**
     * Get an existing ProjectSecretDetectionValidityChecks resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ProjectSecretDetectionValidityChecksState, opts?: pulumi.CustomResourceOptions): ProjectSecretDetectionValidityChecks {
        return new ProjectSecretDetectionValidityChecks(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'gitlab:index/projectSecretDetectionValidityChecks:ProjectSecretDetectionValidityChecks';

    /**
     * Returns true if the given object is an instance of ProjectSecretDetectionValidityChecks.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ProjectSecretDetectionValidityChecks {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ProjectSecretDetectionValidityChecks.__pulumiType;
    }

    /**
     * Set to `true` if the validity checks setting should not be reset to its pre-Terraform value on destroy.
     */
    declare public readonly keepSettingsOnDestroy: pulumi.Output<boolean>;
    /**
     * The ID or full path of the project to change the validity checks setting of.
     */
    declare public readonly project: pulumi.Output<string>;
    /**
     * Whether to enable secret detection validity checks for the project. Requires a GitLab Ultimate license.
     */
    declare public readonly validityChecksEnabled: pulumi.Output<boolean>;

    /**
     * Create a ProjectSecretDetectionValidityChecks resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ProjectSecretDetectionValidityChecksArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ProjectSecretDetectionValidityChecksArgs | ProjectSecretDetectionValidityChecksState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ProjectSecretDetectionValidityChecksState | undefined;
            resourceInputs["keepSettingsOnDestroy"] = state?.keepSettingsOnDestroy;
            resourceInputs["project"] = state?.project;
            resourceInputs["validityChecksEnabled"] = state?.validityChecksEnabled;
        } else {
            const args = argsOrState as ProjectSecretDetectionValidityChecksArgs | undefined;
            if (args?.project === undefined && !opts.urn) {
                throw new Error("Missing required property 'project'");
            }
            if (args?.validityChecksEnabled === undefined && !opts.urn) {
                throw new Error("Missing required property 'validityChecksEnabled'");
            }
            resourceInputs["keepSettingsOnDestroy"] = args?.keepSettingsOnDestroy;
            resourceInputs["project"] = args?.project;
            resourceInputs["validityChecksEnabled"] = args?.validityChecksEnabled;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ProjectSecretDetectionValidityChecks.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ProjectSecretDetectionValidityChecks resources.
 */
export interface ProjectSecretDetectionValidityChecksState {
    /**
     * Set to `true` if the validity checks setting should not be reset to its pre-Terraform value on destroy.
     */
    keepSettingsOnDestroy?: pulumi.Input<boolean | undefined>;
    /**
     * The ID or full path of the project to change the validity checks setting of.
     */
    project?: pulumi.Input<string | undefined>;
    /**
     * Whether to enable secret detection validity checks for the project. Requires a GitLab Ultimate license.
     */
    validityChecksEnabled?: pulumi.Input<boolean | undefined>;
}

/**
 * The set of arguments for constructing a ProjectSecretDetectionValidityChecks resource.
 */
export interface ProjectSecretDetectionValidityChecksArgs {
    /**
     * Set to `true` if the validity checks setting should not be reset to its pre-Terraform value on destroy.
     */
    keepSettingsOnDestroy?: pulumi.Input<boolean | undefined>;
    /**
     * The ID or full path of the project to change the validity checks setting of.
     */
    project: pulumi.Input<string>;
    /**
     * Whether to enable secret detection validity checks for the project. Requires a GitLab Ultimate license.
     */
    validityChecksEnabled: pulumi.Input<boolean>;
}
